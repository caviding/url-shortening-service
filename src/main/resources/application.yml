spring:
  web:
    error:
      include-message: always
  datasource:
    url: jdbc:postgresql://localhost:5432/url_shortener
    username: postgres
    password: admin
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: update
